@model IEnumerable<VehicleEnquiryTechnicalTest.Models.Gearbox>

@{
    ViewBag.Title = "GearIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GearIndex</h2>

<p>
    @Html.ActionLink("Create New", "GearCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GearboxType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GearboxType)
        </td>
        <td>
            @Html.ActionLink("Edit", "GearEdit", new { id=item.ID }) |
            @*@Html.ActionLink("Details", "GearDetails", new { id=item.ID }) |*@
            @Html.ActionLink("Delete", "GearDelete", new { id=item.ID })
        </td>
    </tr>
}

</table>
